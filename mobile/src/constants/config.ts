/**
 * Points at the local backend during development. Override per environment
 * once staging/production API URLs exist (see ../../docs/deployment.md).
 */
export const API_BASE_URL = 'http://localhost:4000/api/v1';
