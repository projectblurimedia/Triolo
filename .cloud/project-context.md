# Project Context (Living Memory)

Update this file whenever product state changes: completed modules, pending work, schema, APIs, decisions. This is the "what" — see [[cloud.md]] for the "how".

## Product Vision

Local-services marketplace for India. MVP launches in Telugu-speaking states, then expands nationwide. Three account types — **User**, **Worker**, **Business** — plus an internal **Admin** system. Full detail: `docs/product-specification.md`.

## MVP Scope Snapshot

- Auth: mobile OTP + JWT. Every self-registered account is a plain `user` (captures at minimum full name + mobile at registration; email and location are optional at the API level — `user-app` still collects both, `partner-app` collects neither, see "Account Model" below) — Worker and Business are **optional capabilities** added afterward (during onboarding or later, from Home's menu), not an exclusive role chosen at signup.
- Worker discovery: broadcast (expanding radius) + manual filtered search.
- Worker booking: PIN-verified arrival, full status pipeline through to rated/closed.
- Business ordering: shopping-list model, per-item availability review, PIN-verified pickup/delivery.
- Admin: verification queues, user/worker/business management, complaints, warnings, suspension/blocking, announcements, audit logs.
- Location: **full GPS coordinates** now captured (registration, Worker service location, Business shop location) — see "Key Technical Decisions" below; this overrides an earlier "no live GPS in MVP" decision. Search itself is still area/village-town-based for now; the captured coordinates are there for radius-based search to build on later.
- Language: core architectural requirement, not a nice-to-have — English + Telugu from MVP, account-level `preferred_language` synced with device storage, first-launch language picker, registration-time language capture, settings-time backend sync. Full model in `docs/localization.md`. No feature ships without both languages.
- No SMS OTP for service completion anywhere — always backend-generated single-use in-app PINs.

## Account Model

Every self-registered account is a plain `user` — not one of an exclusive set of roles chosen at registration. **Worker and Business are optional capabilities**, added via `partner-app`/`registration-web` (not `user-app` — see that app's own Completed Modules entry for why the Home-menu entry points were removed): a `user` account can add a Worker profile (one or more skill categories, experience, structured service address, portfolio photos) and/or a Business profile (shop name, one or more shop categories, structured shop address, shop photos, delivery availability + per-km price). Each capability is its own row (`worker_profiles`/`business_profiles`, owned by the new `workers`/`businesses` backend modules) with its own generated id (worker_id / business_id) and its own `verification_status` (`pending_verification` → `verified`/`rejected`) — independent of `accounts.status`, which is `active` immediately for every self-registered account. `business_staff` (an employee added by a business owner) and `admin` are unrelated to this model — provisioned differently, never self-registered.

This superseded an earlier design (role chosen once at registration, Worker/Business accounts starting `pending_verification`) — `WelcomeScreen`'s three role-selection cards were removed, `RegisterScreen` no longer takes a `role` param at all.

**Account registration itself only strictly requires `fullName`/`mobileNumber`** — `email`/`latitude`/`longitude`/`locationAddress` are all optional at the `POST /auth/register/request-otp` API level (`accounts.email`/`.location_address` were already nullable at the DB level; only the API validation required them, and that requirement is what was relaxed). `user-app`'s `RegisterScreen` still asks for all of them (a consumer account benefits from having a location on file even if it never adds a capability); `partner-app`'s `RegisterScreen` asks only for Full Name + Mobile Number, per explicit product decision — a partner going on to add a Worker/Business capability supplies a far more detailed, structured address in that flow anyway (see the Workers/Businesses migration `0008` note below), so asking for a location twice was pure friction. `Account.email`/`AuthAccount.email` (backend and both mobile apps' local types) are typed `string | null` accordingly; every UI that displays it already guards against (or was fixed to guard against) it being absent.

## Completed Modules

- **Auth** (`backend/src/modules/auth/`): registration OTP request/verify (now captures `email` and location — GPS lat/lng + reverse-geocoded address, or a manual address — alongside full name/mobile; no `role` field, always creates `role: "user"`), login OTP request/verify, JWT access tokens, rotating refresh tokens (selector/verifier split, hashed at rest), `GET /me`, `PATCH /me/language`, account status always `active` immediately on registration, account-level `preferred_language` (enum `app_language`, captured at registration via the OTP record, updatable post-login). 29 unit tests passing (OTP hashing, JWT/refresh mechanics, full service business logic via mocked repository). Repository/API-level integration tests against a real Postgres instance are **not yet written** — none was available in the scaffolding environment; add them before production use (see `docs/testing.md`).
- **Workers** (`backend/src/modules/workers/`): `POST /workers/me/profile` (multipart — **multiple** skill categories (`skillCategories[]`, a worker can select more than one), an `otherSkillDescription` free-text field required when `"other"` is among them, experience years, location, 0-6 portfolio photos uploaded to Cloudinary), `GET /workers/me/profile`, `PATCH /workers/me/profile` (same shape plus `existingPhotoUrls[]` to say which already-uploaded photos to keep — newly uploaded ones are appended, capped at 6; resets `verificationStatus` to `pending_verification` since edited details haven't been reviewed), `DELETE /workers/me/profile`. One profile per account (409 if one already exists on create; 404 on update/delete if none exists). 11 unit tests passing (mocked repository + mocked Cloudinary upload).
- **Businesses** (`backend/src/modules/businesses/`): mirrors Workers exactly, shop-shaped — `POST /businesses/me/profile` (shop name, **multiple** shop categories (`shopCategories[]`), an `otherCategoryDescription` free-text field required when `"other"` is among them, location, 0-6 shop photos, `deliveryAvailable` boolean + `deliveryPricePerKm` required when true), `GET /businesses/me/profile`, `PATCH /businesses/me/profile` (same update/photo-keep/re-verification shape as Workers), `DELETE /businesses/me/profile`. 11 unit tests passing.
- **Image uploads**: shared `multer` middleware (`backend/src/common/middleware/upload.ts`, disk-temp storage, images only, 8MB/file) + `backend/src/common/services/cloudinaryService.ts` (`uploadToCloudinary`, always cleans up the local temp file). Configured via `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` env vars — **the user must supply their own Cloudinary account credentials into their own untracked `backend/.env`**; nothing is hardcoded or shared from elsewhere. If unset, upload attempts fail with a clear `CLOUDINARY_NOT_CONFIGURED` error rather than the server failing to boot.
- **Mobile baseline** (`user-app/`): Expo + TypeScript app scaffolded with the full architecture.md folder structure (screens/components/navigation/services/hooks/state/theme/localization/constants). Working registration + login flow wired end-to-end against the Auth module's API (name/mobile/email/location → OTP request → OTP verify → persisted session), access-token auto-refresh on 401. Post-login app shell: 5-tab bottom navigator (Home, Services, Prepare List, Bazaar, Profile — Prepare List added mid-session as an explicit product decision, a placeholder tab with a `plus` icon; see `.cloud/architecture.md` for the pattern), brand gradient header (`#0055D3` → `#1D76FA`, constant) with a circular icon-only back button on Auth screens and a decorative logo icon + notification/menu actions on Home, light/dark theme (`theme/useThemeColors`, `#F9FCFF`/`#0c0f14` background/text, brand colors constant), Poppins as the app-wide font (Google Sans isn't publicly licensed for third-party use). Home also has a search bar + filter chips (Services/Shops/Top Rated/Nearby, UI-only). Services, Prepare List, and Bazaar are placeholders pending the Worker/Business search/shopping-list modules and a list-preparation module; Profile now holds the account card/language switcher/theme switcher/logout moved out of Home. `WelcomeScreen` is a single "Create Account" CTA (no role picker — see Account Model above); logout shows a dedicated full-screen `LogoutOverlay` for the request's duration instead of an abrupt, feedback-free stack swap; all loading states across the app share one `LoadingIndicator` motif. Typechecks clean and bundles successfully (`expo export --platform web`); backend smoke-tested end-to-end against a live local server (health check, registration OTP request with the new fields, validation rejection, auth-gated Workers/Businesses routes). Not yet run on a real device/emulator. Telugu strings (`user-app/src/localization/te.json`) are machine-translated scaffolding — need native-speaker review before shipping.
  - **Follow-up (same module)**: `HomeMenuModal`'s "Are You a Worker?" / "Do You Have a Shop?" entries (which opened `WorkerProfileModal`/`BusinessProfileModal` in create mode) were removed per explicit product decision — Worker/Business **registration** now happens exclusively via `partner-app`/`registration-web`, so offering it a third time from `user-app` too was redundant. In their place, the drawer's first section (renamed `homeMenu.exploreSection`, "Explore Nearby") now has two discovery entries — "Nearby Workers" and "Nearby Shops" — that open a new `NearbySearchModal` (full-screen, same chrome as `WorkerProfileModal`/`BusinessProfileModal`, `mode`-driven gradient, a real typeable search box in the header) in the corresponding mode. An initial pass had these entries just `navigation.navigate('Services'/'Bazaar')` to the existing placeholder tabs; per follow-up feedback that this should instead be a dedicated search-and-results experience, it's now this modal — with a coming-soon placeholder body, since Worker/Business **discovery** (radius search over each profile's already-captured `latitude`/`longitude`) is still Pending Module 4, not built. `user-app` still lets an account **edit** a Worker/Business profile it already has (`ServicesScreen`/`BazaarScreen`'s own profile cards, and Profile's sections below) — only the *creation* entry point moved out. `ProfileScreen` was also redesigned in the same change: the flat, bordered account-info row was replaced with a `headerGradient` hero card (translucent circular avatar with initials, name/mobile/email in white, an "Active Account" status pill — the same gradient-hero-card identity `partner-app`'s `ProfileScreen`/`MyInfoScreen` already established, see that section's own architecture note), each capability section now has a small heading (`profile.workerSection`/`businessSection`) above its stats row and card, an account with neither capability now sees an explicit `profile.noCapabilitiesTitle`/`noCapabilitiesMessage` card pointing at the Partner app/website instead of a blank screen, and the screen body is now wrapped in a `ScrollView` (it wasn't before) since the hero card plus two capability sections can exceed one screen's height. The hero card's top gap briefly matched the same `paddingVertical`-stacked-on-`ScreenContainer`'s-own-padding bug already documented and fixed in `partner-app` (see "Key Technical Decisions" note on that fix) — the `ScrollView`'s `contentContainerStyle` had its own `paddingVertical: 20` on top of `ScreenContainer`'s uniform 20px, doubling the top gap versus left/right; fixed by dropping the vertical padding there (kept `paddingBottom: 32` alone, for scroll clearance above the floating tab bar). Real push notifications (FCM) were explicitly scoped out of this change at the user's request, since they're currently testing via Expo Go, which doesn't support remote push — still pending, see Pending Modules item 6.
  - **Follow-up (same module)**: Services' and Bazaar's own header menus (`ServicesHeader`/`BazaarHeader`) were redesigned per explicit feedback that they should look and work like Home's own drawer menu, not the compact floating icon dock (`SideDockMenu`, deleted) they'd had — that dock's Filters/Sort/Favorites/History-or-Orders items are gone. Two new components, `components/SideMenuDrawer.tsx` (shared drawer shell/animation, extracted from `HomeMenuModal`'s pattern) and `components/DrawerMenuItem.tsx` (shared row), back the new `ServicesMenuDrawer`/`BazaarMenuDrawer`. Services' drawer: "Get Help" (**Quick Book** — search/book a worker directly; **Post a Problem** — a new user-facing feature name for describing a non-emergency issue so nearby workers can accept it, the broadcast-style discovery already described in the Search Architecture doc, chosen from a few naming options the user was asked to pick from) and "Manage" (**My Bookings**, **Favorite Workers**). Bazaar's drawer mirrors it: "Shop" (**Quick Order**) and "Manage" (**My Orders**, **Favorite Shops**) — no shop-side "post a problem" equivalent was added, since none was requested. None of these destinations exist yet (Worker/Business module remainders, Pending Modules 4/5). `BazaarMenuDrawer` stays the constant brand-blue `headerGradient`, never the orange `SHOP_GRADIENT` — consistent with Bazaar's tab chrome already being blue everywhere else, and with a separate explicit instruction (applied to `NearbySearchModal`) not to introduce that orange into any menu/search surface without being asked.
  - **Follow-up (same module)**: per feedback that every drawer item's icon square looked the same flat color and that tapping one should feel like opening a real screen — each `DrawerMenuItem` in both drawers now gets its own distinct, colorful gradient (blue for Quick Book/Quick Order, purple for Post a Problem, green for My Bookings/My Orders, pink for Favorite Workers/Favorite Shops — still no orange/amber anywhere in this new palette, kept reserved for the Business capability identity), and tapping any item now opens a new full-screen `components/ComingSoonModal.tsx` (same chrome as `NearbySearchModal`, minus the search bar) carrying that item's own gradient/icon through, instead of a "coming soon" toast.
- **Localization** (cross-cutting, not a standalone module — see `docs/localization.md`): first-launch language picker gating the app until confirmed, language re-selectable at registration (sent as part of the registration request), Settings-screen switch that updates UI immediately and syncs to the backend account, account language overriding device state on login, and client-side `error.code` → localized-string mapping (`user-app/src/localization/errorMessages.ts`) so no raw API message is ever shown to a user.
- **Partner app** (`partner-app/`): a second, standalone Expo app — **Triolo Partner** — solving the cold-start supply problem (registering Workers/Businesses without requiring the full consumer app). Same backend/account model as `user-app`, reached capability-first: register/login (same Auth module) → `ChooseCapabilityScreen` (a card per capability — Worker/Business — showing a registration prompt if that capability doesn't exist yet, or a read-only `MyInfoScreen` if it does) → `WorkerRegistrationScreen`/`BusinessRegistrationScreen` (adapted from `user-app`'s `WorkerProfileModal`/`BusinessProfileModal`, create-only for this pass) → `MyInfoScreen` (read-only, no edit/delete yet). Not a monorepo package — a standalone project that copies `user-app`'s proven infra (theme, `apiClient`, stores, shared components) rather than importing it; see `.cloud/architecture.md`'s "Frontend Structure (partner-app/)" for the full rationale and what's deliberately trimmed (no language picker/switcher). `user-app/` was renamed from `mobile/` in the same change (folder/package name only — the "Triolo" brand and `app.json` are unchanged); new `.github/workflows/user-app-ci.yml` (renamed from `mobile-ci.yml`) and `partner-app-ci.yml`. Typechecks clean and bundles successfully (`expo export --platform web`) for both apps. Not yet run on a real device/emulator. Reuses `user-app`'s placeholder icon assets — needs its own distinct branding before shipping.
  - **Follow-up (same module)**: `BusinessRegistrationScreen`/`ChooseCapabilityScreen` no longer use the orange `SHOP_GRADIENT` — both now render the same brand-blue `headerGradient` as Worker registration, per explicit feedback that registration surfaces should look uniform (`user-app`'s Business *capability* identity, `BusinessProfileCard`/`BusinessProfileModal`, is unaffected — that orange distinction stays there, it was never meant to apply to this app's registration forms). And: once a Worker or Business capability reaches `verified`, the app now unlocks a real tab-bar shell (`MainTabNavigator` — Home/Bookings/Earnings/Profile, `@react-navigation/bottom-tabs`' plain default bar) instead of leaving a verified partner stuck on the read-only `MyInfoScreen` with nothing else to do in the app. `MainNavigator` picks `ChooseCapability` vs. `MainTabs` as its `initialRouteName` from `useMyWorkerProfile()`/`useMyBusinessProfile()` (both hooks gained an `enabled` param); `MainTabs` lives inside the same stack so `Profile` can push `WorkerRegistration`/`BusinessRegistration`/`MyInfo` on top of itself. Every tab header carries a `leadingIcon` matching its tab icon, and `ChooseCapabilityScreen`'s own header now carries the `shapes` brand-mark icon too, for a consistent "logo beside the title" across the app.
  - **Follow-up (same module, second pass)**: per feedback to "show tabs that are necessary in future" and make info/profile screens "more attractive" — the placeholder `Search` tab (undefined purpose for a partner) was replaced with `Bookings` and `Earnings`, both honest placeholders mapping directly to already-planned modules (booking/order-flow pipeline; the post-MVP worker earnings dashboard) rather than a generic unexplained tab. `ProfileScreen` and `MyInfoScreen` both gained a shared gradient "hero card" identity treatment (translucent circular avatar, name in white, real drop shadow) plus a reusable `CARD_SHADOW` applied to every card on both screens (previously flat 1px-bordered cards); `MyInfoScreen`'s info rows are now grouped into a shadowed `DetailCard` with dividers and tinted icon badges instead of a flat scroll; `ProfileScreen`'s capability cards gained a `quickFact` line (experience years / shop name) alongside the verification badge. See `.cloud/architecture.md` for full detail.
  - **Follow-up (same module, third pass)**: `MainTabNavigator`'s bar itself is now a direct port of `user-app`'s heavily-iterated floating-pill `CustomTabBar` (new `partner-app/src/components/CustomTabBar.tsx`), replacing the plain default `@react-navigation/bottom-tabs` bar it started with, per explicit feedback that this app's tab bar should look the same as the main app's. Same inline dot-below-icon design, same native-driven crossfade/pop animations — generalized only in that this app has one constant gradient across every tab rather than `user-app`'s per-route color map (Bazaar's orange). No shared package between the two apps' copies, so a future fix to either should be checked against the other.
  - **Follow-up (same module, fourth pass)**: added the settings surface `user-app` already has — every tab header now carries a `bars` menu action opening a new `SettingsMenuModal` (language/theme/logout; `ThemePickerModal`/`LanguagePickerModal` are direct ports of `user-app`'s, working unmodified since `themeStore`/`settingsStore` were already copied verbatim). `ProfileScreen`'s inline logout button was removed accordingly (matches `user-app`'s own Profile screen, which also has no inline logout). Fixed a padding-stacking bug on `ProfileScreen`/`MyInfoScreen`'s hero cards (a redundant `paddingVertical` on top of `ScreenContainer`'s own uniform padding was doubling the top gap vs. left/right). `MyInfoScreen` is no longer read-only — its own header (now self-rendered, not navigator-level, so it can carry the fetched profile) gained a `pen` action opening `WorkerRegistrationScreen`/`BusinessRegistrationScreen` in edit mode; both registration screens now double as edit forms (prefill, `PATCH`, photo add/remove via the existing `ImagePickerField`, a `ConfirmModal`-gated Delete action) mirroring `user-app`'s `WorkerProfileModal`/`BusinessProfileModal` edit-mode branch exactly. Header title font size was confirmed already identical to `user-app`'s (byte-for-byte matching `GradientHeader.tsx`/`typography.ts`) — no change needed there. See `.cloud/architecture.md` for full detail.
  - **Bug fix (same module, fifth pass)**: fixed the reported "sometimes opening the app shows only one tab, no tabs, and just a logout icon" glitch. Root cause: `MainNavigator` only gated on `isLoading` for the Worker/Business profile queries, not `isError` — a transient network failure on either query made it compute `hasVerifiedCapability: false` for an actually-verified user and lock `initialRouteName="ChooseCapability"` (a single non-tab screen with a bare logout-icon header) for the rest of that app session, since `initialRouteName` is a mount-once React Navigation prop. Fixed by adding a proper error/retry view and `key`ing `Stack.Navigator` on `hasVerifiedCapability` so it can recover within the same session. A second, compounding bug was found in the same pass and fixed in **both** `user-app` and `partner-app` (identical copied code): `authStore`/`themeStore`/`settingsStore`'s `onRehydrateStorage` callbacks mutated `state.isHydrated` directly instead of going through a `set()`-based action, which could leave `RootNavigator`'s hydration-gate subscribers un-notified promptly — fixed by adding a real `setHydrated()` action to all three stores in both apps. Full detail in `.cloud/architecture.md`'s Partner-app section.
  - **Follow-up (same module, sixth pass)**: turned out `ChooseCapabilityScreen` showing (registration cards / a Pending badge, no tab bar) was often just correct — an unverified account — not the bug above; but there was no way to see a newly-admin-approved status land without fully killing and relaunching the app, since the profile queries only ever fetched once on mount. Added `RefreshControl` (pull-to-refresh) to `ChooseCapabilityScreen`, and wired React Native's `AppState` into TanStack Query's `focusManager` in **both** apps' `App.tsx` (React Query's default refetch-on-focus does nothing in RN without this — there's no browser `focus` event to rely on). Either trigger refetches the same query `MainNavigator` reads, so its existing `key`-driven remount now picks up a newly-verified status live — no app relaunch needed, just backgrounding/foregrounding the app or a pull-to-refresh.
  - **Follow-up (same module, seventh pass — registration restructure)**: per explicit instruction that registration should be "one place" (same principle already applied to `registration-web`, see that module's own follow-up note) — the standalone `RegisterScreen` (Full Name + Mobile Number only, then a separate `OtpScreen`, then a separate `ChooseCapabilityScreen` → `WorkerRegistrationScreen`/`BusinessRegistrationScreen`) was replaced by two new self-contained screens, `RegisterWorkerScreen`/`RegisterBusinessScreen`, each combining Full Name/Mobile Number (inline OTP verification via a new `components/PhoneVerification.tsx`) with that capability's own fields in one scrollable form; `WelcomeScreen` now offers "Register as a Worker"/"Register as a Business" directly instead of one generic "Create Account" button. Submit is disabled until the mobile number is verified — everything else is fillable in any order. This required a real architectural fix, not just UI: verifying mid-form calls `setSession()`, which normally makes `RootNavigator` instantly swap from `AuthNavigator` to `MainNavigator` (unmounting the very screen the user is filling in) the moment `accessToken` becomes truthy. Fixed by adding a new transient `authStore` flag, `isCompletingRegistration` (set on mount, cleared on unmount or on successful profile submission), and changing `RootNavigator`'s swap condition to `accessToken && !isCompletingRegistration`. Once verified, Full Name/Mobile Number stay visible (disabled) with a "Verified" badge next to the Mobile Number label rather than being replaced by a bare confirmation line — an explicit correction applied to **both** this app's and `registration-web`'s `PhoneVerification` components; `TextField.tsx` gained an optional `labelRight` prop to carry the badge. `OtpScreen`/`LoginScreen` (login only) are unaffected; `useAuthMutations.ts` lost the now-unreachable `useRequestRegistrationOtp`/`useVerifyRegistrationOtp` hooks, and `AuthStackParamList`'s `Otp` route dropped its now-single-valued `mode` union down to just `'login'`. The now-unreachable `RegisterScreen.tsx` was deleted (`LocationPicker.tsx` was already deleted in the previous pass, once `WorkerRegistrationScreen`/`BusinessRegistrationScreen` moved to `AddressPicker`).
- **Registration-web follow-up (same session)**: `RegisterBusinessPage` no longer passes `shop` to `PageHeader`/`PhoneAuthFlow`/`CategoryChips` and no longer uses the `chip--shop`/`button--shop` CSS classes — Business registration is now visually identical to Worker registration. The `shop` prop and `.header--shop`/`.button--shop`/`.chip--active.chip--shop`/`--shop-gradient`/`shopGradient` support were removed from `PageHeader`/`PhoneAuthFlow`/`CategoryChips`/`global.css`/`tokens.ts` entirely, since nothing on this site uses them anymore.
  - **Follow-up (same module)**: `PhoneAuthFlow`'s new-account "details" step asked for Full Name **and** Email, while `partner-app`'s `RegisterScreen` (simplified in this same session — see the Auth module's Account Model note) asks only for Full Name + Mobile Number — flagged as an inconsistency, since this site and `partner-app` serve the identical audience. Fixed by dropping the Email field from `PhoneAuthFlow` entirely, so both now collect exactly the same two fields. `authService.ts`'s `requestRegistrationOtp` param type updated to match (`email`/`latitude`/`longitude`/`locationAddress` all optional, mirroring `partner-app`'s own `authService.ts`), and the placeholder `locationAddress: 'Not provided'` this site used to send (a workaround from when the field was still API-required) was dropped rather than kept as a meaningless stored value now that it's genuinely optional.
  - **Follow-up (same module, restructure)**: per explicit instruction that "registration should be one place" — `RegisterWorkerPage`/`RegisterBusinessPage` were restructured from a staged flow (`PhoneAuthFlow` had to fully complete, hiding the Worker/Business fields, before the actual registration form even appeared) into one single form: Full Name, Mobile Number, and every Worker/Business field are all visible and fillable together from the start. `PhoneAuthFlow` (the old multi-screen gate) was deleted and replaced by `components/PhoneVerification.tsx` — Full Name + Mobile Number with an inline "Verify Mobile Number" button and inline OTP field, embedded in the same form. Only the Submit button is gated on verification (disabled until the mobile number is confirmed, with a caption explaining why), so a visitor can fill in all the Worker/Business details first and verify their mobile number last, exactly as asked — the page doesn't care about field-fill order, only that verification happens before Submit is clickable. Once verified, the existing "already has this capability" check (`GET /workers|businesses/me/profile`) still runs and swaps in the read-only status view if applicable, same as before.
- **Admin module** (`backend/src/modules/admin/`): list + verify only, per confirmed scope — `GET /admin/workers`, `GET /admin/workers/:id`, `PATCH /admin/workers/:id/verification` (`{status: "verified"|"rejected"}`), same three for `businesses`. Every route requires `authorize('admin')`. No repository of its own — depends on `WorkersService`/`BusinessesService`'s new admin-facing methods (`adminListProfiles`/`adminGetProfile`/`adminSetVerificationStatus`), which respects the module-boundary rule; see `.cloud/architecture.md`'s Backend Module Boundaries for the full rationale. `admin` accounts are provisioned via new `backend/scripts/seedAdmin.ts` (`npm run seed:admin -- <mobileNumber> <fullName> [email]`) — never self-registered; login reuses the existing `/auth/login/*` flow, zero new auth code. New unit tests in `workers/service.test.ts`/`businesses/service.test.ts` (the admin-facing methods) and `admin/service.test.ts` (delegation) — 72 backend tests passing total (was 61). Typecheck and lint both clean.
- **Admin website + public registration forms** (`registration-web/`, `admin-web/`): this project's first web frontends — Vite + React + TypeScript, plain CSS. **Two separate sites**, not one combined app (an initial combined `web/` was split per follow-up feedback, so the two audiences ship as genuinely independent deployments): `registration-web/` is `/`, `/worker`, `/business` only — no admin anything, not even a link to it — public, no app install, phone+OTP via `PhoneAuthFlow`, same capability-registration fields as `partner-app`'s screens, browser-native photo/location inputs instead of Expo's. `admin-web/` is `/login` → `/` (dashboard: Workers/Businesses tabs × verification-status filter) → `/workers|businesses/:id` (full profile + Approve/Reject, calling `PATCH .../verification`), gated by `RequireAdmin` (server-side `authorize('admin')` is the real enforcement). No shared package between the two sites or with the mobile apps — brand tokens/`apiClient`/`authStore`/etc. are duplicated and kept in sync manually (see `.cloud/architecture.md`'s "Frontend Structure (registration-web/, admin-web/)"). Both sites use a shared `PageHeader` component — shorter than the mobile apps' `GradientHeader` (no subtitle, less vertical padding), with a FontAwesome `chevron-left` back button rendered beside the title, not above it (`@fortawesome/react-fontawesome` + `@fortawesome/free-solid-svg-icons`, the first icon-library dependency either site has — website-only change, the mobile apps' headers are unaffected). New `.github/workflows/registration-web-ci.yml`/`admin-web-ci.yml` (replacing the earlier combined `web-ci.yml`). Both typecheck clean and `vite build` succeeds. Not yet opened in a real browser from here.

- **Prepare List** (`user-app/src/screens/lists/PrepareListScreen.tsx`): voice-to-list, device-only (never synced to the backend — there is no server-side "list" concept and none is planned). Tap the mic, speak items and quantities in one go ("senagapappu kg minapappu 3/2kg" — a real spoken example), and `utils/parseVoiceList.ts` splits the recognized text into one row per detected "`<item name>` `<quantity>?` `<unit>`" run, defaulting quantity to `1` when a unit is spoken with no number before it, and — deliberately — keeping whatever quantity was recognized **verbatim** rather than mathematically reinterpreting it (a recognized "3/2" stays "3/2", never becomes "1.5"), since speech-to-text output for mixed Telugu/English grocery terms is too noisy to trust a "smart" reinterpretation over what was literally heard. Every row stays freely editable (inline `TextInput`s for name/quantity/unit) and deletable afterward, plus a manual "Add Item" row for typing one in directly — voice is just the fast path in, not the only way to change the list. Items persist via `state/listStore.ts`, a `zustand`/`persist`/`AsyncStorage` store (same convention as `authStore`/`themeStore`/`settingsStore`) — this is genuinely local-only, on-device storage, not a thin client cache of a backend resource. Speech recognition itself is `expo-speech-recognition` (continuous mode) — this is a **native module**, so it needed `app.json`'s `plugins` array updated (permission strings + `androidSpeechServicePackages`) and, unlike the app's OTA-updatable JS changes, required a full new APK build to take effect (see `.cloud/architecture.md`'s Deployment section on what OTA can and can't cover).
  - **Follow-up (bilingual accuracy pass)**: reported as "items merging" — saying "kandipappu kg nnara" (kandipappu, one and a half kg) produced two wrong rows, `Kandipappu - 1 KG` and a bogus `Nnara - 1`, instead of one correct `Kandipappu - 1 1/2 KG`. Two compounding causes, both fixed in `utils/parseVoiceList.ts` and `PrepareListScreen.tsx` without needing a new APK build (pure JS/logic, shipped via `eas update` instead): (1) recognition language switched from picking `en-IN`/`te-IN` off the app's display-language setting to **always `te-IN`** — this app's MVP audience speaks Telugu-with-English-loanwords regardless of which display language happens to be selected, and Google's regional-locale recognizers are specifically trained on that code-switching pattern, unlike `en-IN`, which mangled Telugu quantity words like "nnara" far more often; (2) the parser only understood digit quantities spoken *before* the unit — Telugu naturally puts a quantity word *after* the unit too ("kg nnara"), and the quantity word itself is a word, not a digit. Added `TELUGU_NUMBER_WORDS` (whole numbers 1–10 plus the "N-and-a-half" compounds — "ఒకటిన్నర"/"nnara" etc. — for 1–5, since grocery quantities rarely go higher; both Telugu script and the common Latin-transliterated/STT-misheard renderings of the same words, since recognizers are inconsistent about which one they return) and `TELUGU_UNIT_ALIASES` (కిలో/కేజీ, గ్రాము, లీటరు, డజను, ప్యాకెట్, etc.), merged into the same tables the English words use, and the main parsing loop now looks for a quantity word on *either* side of the unit token, only consuming an after-the-unit word when no before-the-unit quantity was already found (so "rice 2 kg onions" doesn't misread "onions" as a number).
  - **Follow-up (still wrong after the above — the real cause was tokenization, not word order)**: reported again with "kandipappu kgnnara pesarapappu kg" — still merged/misparsed. The actual issue: "1.5kg" spoken naturally in Telugu comes out as **one glued word**, "kgnnara" ("kg" + "nnara" with no pause), not two separate words with a gap between them — so the previous fix's "look for a quantity word on either side of the unit *token*" never even got a chance to run, since `nnara` was never its own token to look at. Added `splitGluedUnitQuantityWord()`: tries every known unit word as both a prefix and a suffix of a token that didn't otherwise match anything, and treats the leftover part as the quantity if it's an exact, whole known quantity word (Telugu or English) — not a fuzzy/partial match, so it can't misfire on an unrelated item name that merely happens to start or end with a unit-like substring. "kandipappu kgnnara pesarapappu kg" now correctly produces `Kandipappu - 1 1/2 KG` and `Pesarapappu - 1 KG`. Again pure JS/logic, shipped via `eas update`.
  - **Follow-up (asked whether a real AI/LLM would do better — explicitly decided against it)**: offered a backend-mediated Claude-parsing alternative (same "API key never reaches the client" pattern already used for Cloudinary — see the Image uploads note below) via `AskUserQuestion`; the user chose to keep improving the free on-device parser instead (no new dependency, no per-request cost, works fully offline once speech is recognized), accepting that it'll keep needing specific fixes as new phrasings surface rather than generalizing on its own. In the same pass, fixed the other reported gap — "telugu items names also combining sometimes" (e.g. "tomatoes and onions 3 kg" fusing into one bogus "Tomatoes Onions" item): connector words ("and", "also", ...) are no longer just dropped during normalization, they're kept as explicit group-boundary markers, so a run of name words gets split back into separate name groups at those boundaries and each group becomes its own item, sharing whatever quantity/unit was found for the group as a whole ("Tomatoes - 3 KG", "Onions - 3 KG"). `utils/parseVoiceList.ts` was rewritten around this grouping model (`groups`/`currentGroup` instead of one flat name-token buffer) — same parsing rules as before, just restructured to support N shared-quantity items instead of assuming exactly one name per unit.
  - **Prepare List also gained a Done/Edit view toggle** in the same round: tapping "Done" swaps the editable rows for a clean read-only numbered list ("1. Kandipappu - 1KG"), with an "Edit List" button to go back; a list that already has items when the screen mounts starts in "done" view, since a returning user most likely wants to look at their finished list rather than land back in edit mode every time.

## Pending Modules (build order, current plan)

1. ~~Auth~~ — done (localization support included)
2. ~~Workers, Businesses (profile-creation slice)~~ — done (capability profiles + image upload; search/booking/order-flow pieces below are still pending)
3. ~~Partner app (capability registration + login + read-only my-info)~~ — done; edit/delete on `MyInfoScreen` still pending, see "Completed Modules" above
4. Worker module remainder (availability, search, booking, PIN verification, ratings)
5. Business module remainder (staff, shopping list, order flow, PIN verification)
6. Notifications (FCM)
7. ~~Admin module — list/verify Worker/Business profiles~~ — done (see "Completed Modules" above); the rest of Admin's planned scope (user/worker/business management, complaints, warnings, suspension/blocking, announcements, audit logs) is still pending
8. ~~Public web sign-up forms for workers/shops (no app install)~~ — done, as its own site `registration-web/`, separate from the Admin website `admin-web/` (see "Completed Modules" above)

Localization is no longer a separate future module — it's a cross-cutting requirement applied to every module above as it's built (English + Telugu keys ship in the same change as any new user-visible text; see `.cloud/development-rules.md` Definition of Done).

## Database Schema

Implemented (`database/migrations/`): `accounts` (`preferred_language app_language`, `email`, `latitude`/`longitude`/`location_address` — this account-level registration location is unchanged, see the `worker_profiles`/`business_profiles` note below for what *did* change), `otp_verifications` (nullable `full_name`/`email`/`latitude`/`longitude`/`location_address`/`preferred_language` for the registration flow — `role` column kept but no longer populated), `refresh_tokens` (selector/verifier split), `worker_profiles` (`skill_categories` — array of `worker_skill_category`, GIN-indexed, plus `other_skill_description`, experience_years, location, portfolio_photo_urls[], verification_status enum), `business_profiles` (shop_name, `shop_categories` — array of `business_shop_category`, GIN-indexed, plus `other_category_description`, location, shop_photo_urls[], `delivery_available` boolean, `delivery_price_per_km`, verification_status enum, shares the `profile_verification_status` enum with worker_profiles). Migration `0007` converted both category columns from single-value to array (backfilling existing rows) and added the other-description/delivery columns. Everything else is still just the outline in `docs/database.md` §"Planned Tables".

**Migration `0008` — structured Worker/Business address** (explicit product decision): `worker_profiles`/`business_profiles`' single free-text `location_address` column was replaced with `area` (nullable — street/locality/landmark), `city`, `district`, `state` (all required), and `pincode` (required, `CHECK (pincode ~ '^[0-9]{6}$')`, India 6-digit) — a manual one-line address wasn't precise enough to ever support area/pincode-based filtering later. `latitude`/`longitude` are unchanged. New indexes: `idx_worker_profiles_city`/`_pincode`, `idx_business_profiles_city`/`_pincode`. This is scoped to the Worker/Business **capability** location only — `accounts.location_address` (captured once at registration, for the account itself) is untouched and out of scope. The migration `TRUNCATE`s both tables first (existing pre-launch test profiles wiped per explicit instruction, since there's no reliable way to auto-split old free-text addresses into the new columns) — every account can simply re-register its Worker/Business profile with the new structured fields. Every layer updated in the same change: `workers`/`businesses` modules' `dto.ts`/`validation.ts`/`repository.ts`/`service.ts` (zod requires `city`/`district`/`state`/`pincode`, `area` optional), both modules' unit tests (72 backend tests still passing), and all four frontends — `user-app`/`partner-app` gained a new shared `AddressPicker` component (City/District/State/Pincode/Area as five always-editable fields; "Use my current location" still reverse-geocodes via `expo-location` and pre-fills what it can, but — unlike the old `LocationPicker`, which is untouched and still used by `RegisterScreen`'s account-level location — editing a field afterward no longer clears the detected lat/lng), `registration-web` gained an `AddressField` (same fields, no reverse-geocoding available there so GPS only captures lat/lng in the background), and `admin-web`'s profile detail page now shows the joined address via a small `formatAddress()` util (duplicated per-app, same "no shared package" convention as everything else cross-app) instead of the raw `locationAddress` string. `WorkerProfileCard`/`BusinessProfileCard`/`MyInfoScreen` all updated to the same `formatAddress()` display.
  - **Follow-up (same module)**: State and District are now dropdowns constrained to India's actual states/districts, not free text, per explicit follow-up feedback. New `constants/indiaLocations.ts` (duplicated across `user-app`/`partner-app`/`registration-web`) holds `INDIA_STATES` (28 states + 8 UTs) and `INDIA_DISTRICTS_BY_STATE` — a point-in-time snapshot flagged in its own doc comment for periodic re-verification, since district boundaries change. District options cascade off the selected state and reset if they're no longer valid for a newly-picked state. Mobile apps got a new shared `SelectField`/`SelectModal` (a `TextField`-styled tap target opening a full-screen searchable list, matching `NearbySearchModal`'s chrome); `registration-web` just uses native `<select>`/`<option>` (new `.field select` CSS rule). GPS-based auto-fill on `AddressPicker` now only sets State/District when the reverse-geocoded name actually matches an entry in the fixed list, leaving it blank for manual selection otherwise rather than storing an unlisted value.
  - **Bug fix (same module, follow-up pass)**: District often wasn't auto-selecting after "Use my current location," reported from `partner-app`. Root cause: `matchInList`'s exact (case-insensitive) comparison was too strict for real reverse-geocoder output — Android/iOS geocoders frequently return a district-level name with a "District" suffix, different spacing, or in a different field (`subregion` vs `district` vs `city`) than this hand-typed list expects, so an exact match often failed silently with no feedback. Fixed by (1) normalizing both sides (strip a trailing "district" word, collapse whitespace) before comparing, (2) falling back to substring containment either direction when the normalized exact match fails, (3) trying `subregion`, then `district`, then `city` in turn as the candidate source instead of committing to one, and (4) showing a toast ("we detected your state but couldn't match your district — please select it") whenever auto-fill genuinely can't determine the district, so the gap is visible instead of silently confusing. Applied identically to both `user-app`'s and `partner-app`'s `AddressPicker` copies.
  - **Follow-up (partner-app only)**: `partner-app`'s `RegisterScreen` (account creation) now asks only for Full Name + Mobile Number — email and location were dropped from this screen per explicit product decision, since a partner going on to add a Worker/Business capability supplies a far more detailed structured address in that flow anyway, making a second, cruder location prompt pure friction. This only required relaxing validation, not a schema change (`accounts.email`/`.location_address` were already nullable) — see the Account Model section above for the full detail. `user-app`'s own `RegisterScreen` is unchanged and still asks for both. `partner-app`'s now-unused `LocationPicker.tsx` component was deleted (nothing else in that app used it once `AddressPicker` took over Worker/Business forms and this screen dropped location entirely).

**pg gotcha fixed in the same change**: node-postgres only auto-parses array columns of *built-in* types — arrays of custom enum types (like `worker_skill_category[]`) come back as the raw Postgres array-literal string (`"{electrician,other}"`) rather than a JS array, since pg has no registered type parser for custom-enum-array OIDs. Fixed via a small `parsePgArray` util (`backend/src/common/utils/pgArray.ts`) applied in both repositories' row-mapping functions. Also fixed in this change: the shared `pg.Pool` (`backend/src/config/database.ts`) had no `.on('error', ...)` handler, so an idle-connection reset from Supabase (routine, happens periodically) crashed the entire Node process via Node's unhandled-`'error'`-event contract — this was the actual root cause of intermittent "something went wrong" failures reported during registration/profile submission, since the whole server would silently die and stay dead until manually restarted. Now logs and survives it.

## APIs

Implemented under `/api/v1/auth`: `register/request-otp` (fullName, mobileNumber, email, latitude?, longitude?, locationAddress, preferredLanguage?), `register/verify-otp`, `login/request-otp`, `login/verify-otp`, `refresh`, `logout`, `me`, `PATCH me/language`. Implemented under `/api/v1/workers`: `POST me/profile` (multipart), `GET me/profile`, `PATCH me/profile` (multipart, edits an existing profile — resets `verificationStatus` to `pending_verification`), `DELETE me/profile`. Implemented under `/api/v1/businesses`: same shape, shop-flavored. Full request/response detail in `docs/api.md`.

## Key Technical Decisions

- Modular monolith, feature-based backend modules (routes/controller/service/repository/validation/dto/tests each).
- PostgreSQL as default DB.
- PIN verification (not SMS OTP) for all service-completion steps; PINs are backend-generated, single-use, per-booking/per-order.
- **Full GPS coordinates captured** (registration location, Worker service location, Business shop location) — a confirmed, deliberate override of an earlier "no live GPS in MVP, area-based only" decision. Search itself remains area/village-town-based for now; the coordinates are stored for radius-based search to build on later without another migration.
- Account model: every self-registered account is `role: "user"`; Worker/Business are additive capabilities with their own tables/verification status, not an exclusive role — see "Account Model" above.
- Image uploads: backend-mediated Cloudinary (multer → temp disk → Cloudinary → cleanup), never exposing the API secret to the client. Each user supplies their own Cloudinary credentials via env vars.
- Business staff share one dashboard with real-time sync; permission-scoped actions.
- Order/booking cancellation locks once business review / worker-PIN-verification begins; only business/admin can cancel past that point.
- Localization: language is a first-class account attribute (`accounts.preferred_language`), not a purely client-side setting; client never displays raw API error messages, only `error.code` mapped to a localized string.
- **Editing a Worker/Business profile keeps its existing `verificationStatus` — it no longer resets to `pending_verification`.** This reverses an earlier design (documented here for a while: "editing resets to pending, since a verified profile changing its details hasn't been reviewed in its new form") per explicit follow-up feedback that partner-app's edit flow shouldn't demote an already-verified profile just for adding a photo. `WorkersService.updateProfile`/`BusinessesService.updateProfile` now pass `verificationStatus: existing.verificationStatus` through to the repository instead of hardcoding `'pending_verification'` — shared logic, so this applies identically to `user-app`'s `WorkerProfileModal`/`BusinessProfileModal` edit mode and `partner-app`'s `WorkerRegistrationScreen`/`BusinessRegistrationScreen` edit mode. Admin still gets visibility into edits, but as plain information rather than a re-review gate: `worker_profiles`/`business_profiles.updated_at` (already existed, already returned by the admin-facing `WorkerProfileWithAccount`/`BusinessProfileWithAccount` types) is now also typed and displayed in `admin-web` — "Registered `<date>`" plus, when different, "Last updated `<date>`" on `AdminProfileDetailPage`, and a compact "Edited `<date>`" caption on `AdminDashboardPage`'s list rows when a profile has been edited since its original submission. Deleting a profile is still a hard delete (no soft-delete/archive), reachable from Home menu → the profile form's own "Delete" action, or the `pen` edit button on `WorkerProfileCard`/`BusinessProfileCard` (user-app) / `MyInfoScreen`'s header (partner-app) — all entry points open the same edit-mode form with the existing profile passed in.

- **Backend is deployed on Vercel as a serverless function** (`https://triolo-backend.vercel.app`), not Render — Render was tried first but asked for card details even for its free tier, so the user switched. `backend/api/index.js` is the serverless entry point: a plain JS file (deliberately not TS, so there's no path-alias resolution for Vercel's function bundler to get wrong) that `require()`s the already-`tsc`+`tsc-alias`-compiled `dist/app.js` (built via `backend/vercel.json`'s `buildCommand: npm run build`) and re-exports `createApp()`'s return value directly — an Express app is itself a valid `(req, res) => void` handler, so no adapter is needed. `vercel.json`'s `rewrites` sends every path to `/api/index` while leaving the original `req.url` intact, so the Express app's own internal routing (`/health`, `/api/v1/...`) works unchanged from how it behaves under `server.ts`'s plain `app.listen()` locally. Deployed via `vercel link --project triolo-backend` + `vercel env add ... --value ... --yes` (11 env vars — same values as `backend/.env` except **freshly generated `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET`**, not the local dev placeholders) + `vercel deploy --prod --yes`, all non-interactively since this environment has no real TTY for CLI prompts (see the EAS keystore note elsewhere in this file for the same underlying constraint). `backend/vercel.json`/`api/index.js` and root `.gitignore`'s `.vercel/` entry are committed; the Vercel project link itself (`.vercel/project.json`) is not.
  - **Gotcha that broke the first deploy**: Supabase's *direct* connection hostname (`db.<project-ref>.supabase.co`, the one `backend/.env` uses for local dev) is IPv6-only on Supabase's free tier, and Vercel's serverless Node runtime has no outbound IPv6 support in the `iad1` region — every DB query failed with `getaddrinfo ENOTFOUND db.<ref>.supabase.co`, even though `/health` (no DB touch) returned 200 fine, which is what made it look like a generic "deployment is broken" issue rather than a DNS/connectivity one specifically. Fixed by switching the deployed `DATABASE_URL` (Vercel env var only — `backend/.env`'s local value is untouched and still uses the direct connection, which works fine from a long-lived local Node process) to Supabase's **connection pooler** (Supavisor) string instead: `postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres` — found under Supabase dashboard → Project Settings → Database → Connection pooling → Transaction mode. The pooler hostname is IPv4-reachable specifically to solve this exact serverless/edge connectivity problem; this is a standard, documented Vercel+Supabase pairing gotcha, not specific to this codebase. Any *other* service ever deployed to a serverless/edge platform (Vercel, Lambda, Cloudflare Workers, etc.) against this same Supabase project must use the pooler connection string too, not the direct one.
  - `user-app/src/constants/config.ts`'s `API_BASE_URL` now branches on `__DEV__`: the LAN IP in Expo Go/dev-client (unchanged), or `process.env.EXPO_PUBLIC_API_URL` (falling back to a hardcoded `FALLBACK_PROD_URL` matching the Vercel URL) in a built app — same env-driven pattern already proven in a sibling project (CampuSphere's `serverConfig.js`). `EXPO_PUBLIC_API_URL` itself is set per build profile in `user-app/eas.json` (`preview`/`production`), not hardcoded in app source, so pointing a future build at a different backend is a one-line `eas.json` edit.
- **Backend dev workflow gotcha (not a code bug, but has repeatedly looked like one)**: `backend/package.json`'s `dev` script (`tsx watch src/server.ts`) auto-reloads on source changes — that's the mode to run while developing. `npm start` (`node dist/server.js`) does **not** auto-reload; it serves whatever was last compiled by `npm run build`, which can silently drift out of sync with `src/` for days. This was caught directly: `src/modules/auth/validation.ts` had `email`/`locationAddress` made optional (see Account Model above), but a live request against the running server still rejected them as `Required` — the running process turned out to be serving a `dist/` build compiled *before* that change (confirmed by comparing `dist/modules/auth/validation.js`'s content and mtime against `src/modules/auth/validation.ts`). Rebuilt `dist/` to resolve it. If a backend behavior change doesn't seem to be taking effect despite the source clearly being correct, check which mode the running process is in before assuming the code is wrong — restart under `npm run dev`, or `npm run build` again, before debugging further.

## Open Questions / Not Yet Decided

- Payment method for MVP: cash / external UPI assumed, no in-app payment gateway yet.
- Exact list of supported languages beyond English/Telugu.
- Hosting target (VPS vs AWS) — deferred until deployment phase.
- Whether Worker/Business capability can be added more than once per account in different categories (currently one profile per account, enforced by a unique `account_id` constraint on each table).
