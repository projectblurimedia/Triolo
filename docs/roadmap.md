# Roadmap

Canonical roadmap lives in [[.cloud/roadmap.md]] (phases + module build order) since it's read automatically by Claude every session. This file exists for discoverability from `docs/`; keep both in sync if you edit one.
