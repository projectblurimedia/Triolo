# Dependencies
node_modules/
.pnp
.pnp.js

# Env
.env
.env.*
!.env.example

# Build output
dist/
build/
out/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing / coverage
coverage/

# Multer disk-storage staging dir (temp upload files, cleaned up after each Cloudinary upload)
backend/temp/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# React Native / Expo
.expo/
.expo-shared/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
user-app/ios/
user-app/android/app/build/
partner-app/ios/
partner-app/android/app/build/

# Docker
docker/*.env

# Vercel (project link — environment-specific, not shared via git)
.vercel/

# Local EAS build signing credentials (self-signed Android keystore + its passwords, per
# app — user-app/credentials/, partner-app/credentials/, etc.) — never committed,
# generated once per app and kept only on this machine.
credentials/
credentials.json
